<!DOCTYPE html>
<html lang="en">
<head>
  <title>Vocabularies Overview</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="./css/bootstrap.min.css">
  <link rel="stylesheet" href="./css/custom.css">
</head>
  <body>
    <div class="container-fluid">
      <h1><a href="index.html" title="">Vocabularies</a></h1>
      {{#vocs}}
        <hr>
        <div class="voc-info">
          {{#id}}<h2>{{id}}</h2>{{/id}}
          {{#base}}<p><b>Base:</b> {{base}}</p>{{/base}}
          {{#usage}}<p><b>Usage:</b> {{usage}}</p>{{/usage}}
          {{#properties}}<p><b>Properties:</b> <samp>{{properties}}</samp></p>{{/properties}}
          <p><b>Classes:</b>{{^classes}} None{{/classes}}</p>
          <ul>
            {{#classes}}
              <li><a href="{{page}}" title="{{clsId}}">{{name}}</a></li>
            {{/classes}}
          </ul>
        </div>
      {{/vocs}}
    </div>
  </body>
</html>
